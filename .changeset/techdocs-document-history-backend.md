---
'@backstage/plugin-techdocs-backend': minor
---

Added optional document history APIs backed by GitHub and GitLab. When `techdocs.history.enabled` is true, TechDocs can return commit history, blame, tags, file content at a revision, and diffs for documentation source files.
